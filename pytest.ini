[pytest]
testpaths = tests
# Add the project root to sys.path so `from app import ...` works
# without having to set PYTHONPATH manually.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
